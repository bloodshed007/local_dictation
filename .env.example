# Default: local CUDA transcription. No API key required.
STT_PROVIDER=faster-whisper
STT_MODEL=small
STT_DEVICE=cuda
STT_COMPUTE_TYPE=float16
STT_LANGUAGE=en
STT_SPEECH_RMS_THRESHOLD=650
# Silence (ms) that auto-finalizes a segment. Both this and the RMS threshold
# can also be tuned live in the UI (saved to settings.json, which wins).
STT_FINAL_SILENCE_MS=700
STT_HOLD_KEY=f8
# Optional name substring; leave blank to follow the Windows default at startup.
STT_MIC_DEVICE=

# Lightweight local CPU fallback:
# STT_PROVIDER=vosk

# Optional cloud provider:
# STT_PROVIDER=deepgram
# DEEPGRAM_API_KEY=replace_with_your_deepgram_api_key
